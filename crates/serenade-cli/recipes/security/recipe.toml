id = "security"
description = "Security package config scaffold (firewall, voters, app-owned authenticators)"

[cargo]
dependencies = []

[[files]]
src = "files/config/packages/security.toml"
dest = "config/packages/security.toml"

[hints]
bundles = ["SecurityBundle"]
note = "Writes config/packages/security.toml with enabled = false until the app wires authenticators."
