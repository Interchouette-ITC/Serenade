[package]
name = "serenade-mailer"
description = "Email message, Mime multipart tree, and Null/File/SMTP transports"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
rust-version.workspace = true

[features]
default = ["smtp"]
## Enable [`crate::SmtpTransport`] via lettre.
smtp = ["dep:lettre"]
## Enable EspHttpTransport (SendGrid-class HTTP mail API; apps own the HTTP client).
esp = ["dep:serde_json"]

[dependencies]
serenade-di = { workspace = true }
thiserror = { workspace = true }
serde_json = { workspace = true, optional = true }
lettre = { version = "0.11", optional = true, default-features = false, features = [
    "smtp-transport",
    "builder",
    "hostname",
    "rustls-tls",
] }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
