# serenade-form

HTML forms: bind request, CSRF by default, HTML escape on render.

Apps declare fields and constraints; Serenade owns CSRF and XSS-safe escaping.
See `docs-dev/FORMS.md` and `docs-dev/SECURITY.md`.
