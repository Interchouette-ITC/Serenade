# serenade-messenger

Synchronous message bus, middleware chain, and async transport adapters.

Use for command/query-style dispatch inside the process; validation hooks can
reject messages before handlers run.
