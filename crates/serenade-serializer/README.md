# serenade-serializer

DTO serialization with JSON codecs and a normalizer registry.

Normalize domain objects to arrays/maps for HTTP or messenger payloads without
tying the kernel to a single serde layout.
